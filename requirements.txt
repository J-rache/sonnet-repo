# Core runtime
anthropic>=0.30.0
fastapi>=0.111.0
uvicorn>=0.30.0
pydantic>=2.0.0
pyyaml>=6.0
aiofiles>=23.0.0
httpx>=0.27.0

# Embeddings (real vector similarity, no external API needed)
scikit-learn>=1.4.0
numpy>=1.26.0

# Dev / test
pytest>=8.0.0
pytest-asyncio>=0.23
